basePath: /api/v1
info:
  contact: {}
  description: Internal API
  title: be2 API
  version: "1.0"
paths:
  /addaddaddress:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses: {}
      summary: Add Address
      tags:
      - address
swagger: "2.0"
